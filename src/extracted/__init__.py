"""
Extracted modules from sibling projects for side-by-side comparison.

These are NOT wired into the runtime. They exist so we can diff against
the canonical modules in src/ and decide what to merge.
"""
